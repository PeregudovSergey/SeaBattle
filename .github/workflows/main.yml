name: CHECK-RUN

on:
  push:
    branches: [ '**' ]
  pull_request:
    branches: [ dev ]

jobs:
  RUN-UBUNTU:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Check cmake version
      run: cmake --version
  
    - name: Build
      run: cd part2 && mkdir build && cd build && cmake .. && make

  RUN-WINDOWS:
      runs-on: windows-latest
    
      steps:
      - uses: actions/checkout@v2
      - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
        run: cd part2 && cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}
 
      - name: Build
    # Build your program with the given configuration
        run: cd part2 && cmake --build ${{github.workspace}}/build --config ${{env.BUILD_TYPE}}

      #- name: Build
        #run: cd part2 && mkdir build && cd build && cmake build .. && cmake --build .
